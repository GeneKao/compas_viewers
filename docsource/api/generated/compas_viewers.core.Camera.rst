.. rst-class:: detail

Camera
==========================

.. currentmodule:: compas_viewers.core

.. autoclass:: Camera

    
    

    .. rubric:: Attributes

    .. autosummary::
    
        ~Camera.aspect
        ~Camera.distance
        ~Camera.distance_delta
        ~Camera.far
        ~Camera.fov
        ~Camera.near
        ~Camera.rotation_delta
        ~Camera.rx
        ~Camera.rz
        ~Camera.staticMetaObject

    .. rubric:: Inherited Attributes

    .. autosummary::
    
        ~Camera.destroyed
        ~Camera.objectNameChanged

    
    

    
    

    .. rubric:: Methods

    .. autosummary::
        :toctree:

    
        ~Camera.__init__
        ~Camera.aim
        ~Camera.focus
        ~Camera.rotate
        ~Camera.translate
        ~Camera.zoom
        ~Camera.zoom_extents
        ~Camera.zoom_in
        ~Camera.zoom_out

    .. rubric:: Inherited Methods

    .. autosummary::
        :toctree:

    
        ~Camera.blockSignals
        ~Camera.childEvent
        ~Camera.children
        ~Camera.connect
        ~Camera.connectNotify
        ~Camera.customEvent
        ~Camera.deleteLater
        ~Camera.disconnect
        ~Camera.disconnectNotify
        ~Camera.dumpObjectInfo
        ~Camera.dumpObjectTree
        ~Camera.dynamicPropertyNames
        ~Camera.emit
        ~Camera.event
        ~Camera.eventFilter
        ~Camera.findChild
        ~Camera.findChildren
        ~Camera.inherits
        ~Camera.installEventFilter
        ~Camera.isSignalConnected
        ~Camera.isWidgetType
        ~Camera.isWindowType
        ~Camera.killTimer
        ~Camera.metaObject
        ~Camera.moveToThread
        ~Camera.objectName
        ~Camera.parent
        ~Camera.property
        ~Camera.receivers
        ~Camera.registerUserData
        ~Camera.removeEventFilter
        ~Camera.sender
        ~Camera.senderSignalIndex
        ~Camera.setObjectName
        ~Camera.setParent
        ~Camera.setProperty
        ~Camera.signalsBlocked
        ~Camera.startTimer
        ~Camera.thread
        ~Camera.timerEvent
        ~Camera.tr

    
    